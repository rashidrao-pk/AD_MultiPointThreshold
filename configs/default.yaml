device: cpu

data:
  name: MVTec
  dataset_root: "/home/hakimmics/data/mvtec_anomaly_detection"
  category: zipper
  img_size: 128
  batch_size: 16
  num_workers: 2
  pin_memory: true


model:
  name: vaegan
  checkpoint_root: "models/vaegan"
  latent_dim: 64
  pretrained: true

scoring:
  method: reconstruction_quantiles
  quantiles: [0.5, 0.75, 0.9, 0.95, 0.99]



threshold:
  method: multi_point
  percentile: 95 # 
  decision_rule: any 




# decision rules for multi-point thresholding:
# - any: if any of the scores exceed the threshold, classify as anomaly
# - all: if all scores exceed the threshold, classify as anomaly
# - majority: if more than half of the scores exceed the threshold, classify as anomaly